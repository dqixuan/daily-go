package Palindrome

import (
	"fmt"
	"testing"
)

func Test_matches(t *testing.T) {
	fmt.Println(matches(2))
	//arr := []int{2, 1, 4, 3}
	//quickSort(arr, 0, 3)
	//fmt.Println(arr)
	arr := make([]int, 10)
	arr = append(arr, 11)
	fmt.Println(arr)
}